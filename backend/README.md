# Backend (Laravel 11 API)

## Setup
1. `cd backend`
2. `composer install`
3. Configure `.env` with PostgreSQL + Supabase credentials.
4. `php artisan migrate`
5. `php artisan serve`

## Required packages
- `firebase/php-jwt`
- `barryvdh/laravel-dompdf`

## Tests
`php artisan test`
